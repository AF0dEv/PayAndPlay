@model PayAndPlay.Models.MusicaInPlayList

@{
    ViewData["Title"] = "Create";
}
<div style="padding-top: 70px;">
<h1>Create</h1>

<h4>MusicaInPlayList</h4>
</div>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="MusicaId" class="control-label"></label>
                <select asp-for="MusicaId" class ="form-control" asp-items="ViewBag.MusicaId"></select>
            </div>
            <div class="form-group">
                <label asp-for="PlayListId" class="control-label"></label>
                <select asp-for="PlayListId" class ="form-control" asp-items="ViewBag.PlayListId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
