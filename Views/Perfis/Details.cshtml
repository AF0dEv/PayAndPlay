@model PayAndPlay.Models.Perfil

@{
    ViewData["Title"] = "Details";
}
<div style="padding-top: 70px;">
<h1>Details</h1>

<h4>Perfil</h4>
</div>
<div>
    @{
        Html.RenderPartial("_Alert");
    }
    <dl class="row">
        <dt class = "col-sm-2">
            Tipo Perfil
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.ID) - <span style="color:lightgray">@Html.DisplayFor(model => model.Tipo_Perfil)</span>
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.ID">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
